
\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\section{Общая схема архитектуры (System Context / Container Diagram)}\label{KT-2.1_Architecture-ux43eux431ux449ux430ux44f-ux441ux445ux435ux43cux430-ux430ux440ux445ux438ux442ux435ux43aux442ux443ux440ux44b-system-context--container-diagram}

ProjectV --- десктопное приложение реального времени (нативный бинарник под Windows и Linux). Архитектура однопроцессная, не клиент-серверная: вся логика, рендер и физика выполняются в одном процессе на стороне пользователя.

\textbf{Подсистемы первого уровня:}

\begin{longtable}[]{@{}lll@{}}
\toprule\noalign{}
Подсистема & Зона ответственности & Зависимости \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\texttt{Платформа} & Окно (SDL3), ввод, жизненный цикл & SDL3 \\
\texttt{Воксельный\ мир} & Источник истины для всех воксельных данных & --- \\
\texttt{Физика} & Walk-персонаж, столкновения со статической геометрией & Jolt \\
\texttt{Аудио} & Фоновая музыка, плейлист, горячая замена треков & miniaudio \\
\texttt{ECS} & Зеркало состояния мира для запросов и симуляций & flecs \\
\texttt{Реестр\ ассетов} & glTF / Draco / meshopt-ассеты, горячая перезагрузка & fastgltf, draco, meshopt \\
\texttt{Рендерер} & Мешинг, освещение, тени, TAA, ray march, debug-оверлей & Vulkan 1.4 \\
\texttt{GPU} & Все вычислительные и графические ресурсы & --- \\
\end{longtable}

Поток данных (упрощённо): SDL3 → события платформы → \texttt{InputState} → \texttt{AppState} → \texttt{VoxelWorld} (источник истины) → \texttt{Физика} (Jolt) + \texttt{ECS} (зеркало) + \texttt{Реестр\ ассетов} → \texttt{Рендерер} (подготовка на CPU) → \texttt{Vulkan} → GPU.

\textbf{Графическая схема архитектуры (System Context / Container Diagram):}

\pandocbounded{\includegraphics[keepaspectratio]{architecture_diagram.png}}

\section{Стек технологий с обоснованием}\label{KT-2.1_Architecture-ux441ux442ux435ux43a-ux442ux435ux445ux43dux43eux43bux43eux433ux438ux439-ux441-ux43eux431ux43eux441ux43dux43eux432ux430ux43dux438ux435ux43c}

\textbf{Языки и сборка:}

\begin{longtable}[]{@{}lll@{}}
\toprule\noalign{}
Технология & Версия & Назначение \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
C++26 & ISO/IEC 14882:2026 & Основной язык реализации \\
C23 & ISO/IEC 9899:2024 & Подмодули с C-API (Jolt, draco, meshopt) \\
CMake & 3.30+ & Система сборки \\
Ninja & 1.13+ & Бэкенд сборки (быстрые инкрементальные сборки) \\
Clang 22 & 22.1.6 & Компилятор (мультиплатформенный, C++26-фичи) \\
\end{longtable}

\textbf{Платформа и графика:}

\begin{longtable}[]{@{}lll@{}}
\toprule\noalign{}
Технология & Версия & Назначение \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
SDL3 & 3.4.10 & Платформенный слой (окно, ввод) \\
Vulkan 1.4 & 1.4.350 & Графический API \\
volk & pinned & Vulkan-загрузчик (runtime-загрузка) \\
VMA & pinned b3cbbb43 & Менеджер памяти GPU (chronos-group) \\
GLSL → SPIR-V & 4.5 / 1.4 & Шейдеры (автокомпиляция через glslc) \\
RenderDoc & 1.30+ & GPU-отладчик \\
\end{longtable}

\textbf{Сторонние библиотеки:}

\begin{longtable}[]{@{}lll@{}}
\toprule\noalign{}
Технология & Версия & Назначение \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
flecs & 2.2.0 & ECS (header-only, MIT) \\
JoltPhysics & pinned & Физика (детерминированная) \\
Tracy & 0.11+ & Профилирование (CPU + GPU-зоны) \\
fmt & pinned & Типобезопасное форматирование \\
glm & pinned & Математика (header-only) \\
fastgltf & pinned & glTF-загрузчик (header-only, simdjson backend) \\
draco & pinned & Сжатие мешей (Google decoder-only) \\
meshoptimizer & pinned & Оптимизация мешей (vertex cache, перерисовка) \\
miniaudio & pinned & Аудио (header-only MIT, single-file) \\
nlohmann/json & 3.11.3 (FetchContent) & JSON-парсинг (header-only MIT) \\
Dear ImGui & opt-in (OFF по умолчанию) & ImGui UI \\
\end{longtable}

\textbf{Итого:} 22 подмодуля + 1 FetchContent (nlohmann/json) + системные библиотеки (zlib, png, jpeg для текстур --- opt-in).

\section{Структуры данных}\label{KT-2.1_Architecture-ux441ux442ux440ux443ux43aux442ux443ux440ux44b-ux434ux430ux43dux43dux44bux445}

\begin{longtable}[]{@{}ll@{}}
\toprule\noalign{}
Структура & Поля (типы) \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\texttt{VoxelWorld} & \texttt{voxels:\ vector\textless{}uint8\_t\textgreater{}}, \texttt{chunks:\ vector\textless{}...\textgreater{}} \\
\texttt{VoxelChunk} & \texttt{min/maxExclusive:\ Int3}, \texttt{rebuildQueued:\ bool}, \texttt{nonAirVoxelCount:\ Int} \\
\texttt{VoxelMaterial} (enum) & \texttt{Air=0,\ Glass=1,\ Fluid=2,\ FloorWhite=3,\ FloorGray=4} (uint8) \\
\texttt{VoxelMaterialVisual} (struct) & \texttt{baseColor:\ vec4}, \texttt{surface:\ \{AO,\ roughness,\ metallic,\ reflectance\}} \\
\texttt{AppState} (root) & см. §4 \\
\texttt{WorldState} & \texttt{voxelWorld:\ unique\_ptr\textless{}VoxelWorld\textgreater{}}, \texttt{requestedScenePreset:\ VoxelScene} \\
\texttt{SceneResources} & \texttt{chunkDescriptors:\ vector\textless{}...\textgreater{}}, \texttt{sceneChunkVoxelData:\ ...} \\
\texttt{InteractionState} & \texttt{selection:\ RaycastHit}, \texttt{placementVoxel:\ Int3}, \texttt{hitNormal:\ vec3}, \texttt{hitDistance:\ float} \\
\texttt{PackedSceneChunkDescriptor} & 64 B std430 --- см. детали ниже \\
\texttt{PackedSceneVoxelFace} & 16 B --- см. детали ниже \\
\texttt{VoxelSceneLighting} & 9 × \texttt{array\textless{}float,4\textgreater{}} --- см. детали ниже \\
\end{longtable}

\textbf{Детализация критичных структур:}

\begin{itemize}
\tightlist
\item
  \textbf{\texttt{PackedSceneChunkDescriptor}} (64 B, std430, см. \texttt{src/core/Types.hpp:110-118}):

  \begin{itemize}
  \tightlist
  \item
    \texttt{chunkOrigin:\ array\textless{}int32,\ 4\textgreater{}} (offset 0) --- начало чанка в мировых координатах;
  \item
    \texttt{chunkExtentAndNonAir:\ array\textless{}uint32,\ 4\textgreater{}} (offset 16) --- размеры + счётчик non-air;
  \item
    \texttt{voxelDataInfo:\ array\textless{}uint32,\ 4\textgreater{}} (offset 32) --- индексы в \texttt{sceneChunkVoxelData};
  \item
    \texttt{drawRanges:\ array\textless{}uint32,\ 4\textgreater{}} (offset 48) --- диапазоны draw call\textquotesingle ов.
  \end{itemize}
\item
  \textbf{\texttt{PackedSceneVoxelFace}} (16 B, см. \texttt{src/core/Types.hpp:96-108}):

  \begin{itemize}
  \tightlist
  \item
    \texttt{voxelCoord,\ axisMask,\ materialIndex,\ lightingData,\ packedExtents:\ uint32\_t} --- координаты воксела, маска осей, материал, освещение, packed extents.
  \end{itemize}
\item
  \textbf{\texttt{VoxelSceneLighting}} (9 × \texttt{vec4}, контракт с GLSL \texttt{SceneLightingBuffer}):

  \begin{itemize}
  \tightlist
  \item
    \texttt{skyColorAndFogDensity}, \texttt{horizonColorAndFogStart}, \texttt{groundColorAndFogMax}, \texttt{sunColorAndIntensity}, \texttt{sunDirectionAndWrap}, \texttt{postProcess}, \texttt{sunShadowParams}, \texttt{sunContactShadowParams}, \texttt{ambientOcclusionParams}.
  \end{itemize}
\end{itemize}

\textbf{Связи (FK):}

\begin{itemize}
\tightlist
\item
  \texttt{VoxelWorld} → \texttt{VoxelChunk} (1:N) --- каждый чанк принадлежит одному миру.
\item
  \texttt{VoxelChunk} ↔ \texttt{VoxelWorld} (1:1) --- границы (\texttt{min/max}) задают подмножество мировых координат.
\item
  \texttt{VoxelMaterialVisual} → \texttt{VoxelMaterial} (N:1) --- несколько визуалов могут ссылаться на один материал.
\item
  \texttt{WorldState} → \texttt{VoxelWorld} (1:1) --- \texttt{unique\_ptr}.
\item
  \texttt{SceneResources} → \texttt{VoxelChunk} (1:1) --- \texttt{chunkDescriptors{[}i{]}} соответствует \texttt{chunks{[}i{]}}.
\item
  \texttt{PackedSceneChunkDescriptor} → \texttt{VoxelChunk.min} (1:1) --- \texttt{chunkOrigin} задаёт начало чанка.
\item
  \texttt{PackedSceneVoxelFace} → \texttt{VoxelWorld.voxels} (N:1) --- грань ссылается на конкретный воксель.
\item
  \texttt{VoxelSceneLighting} --- общий контракт с шейдерами (GLSL \texttt{SceneLightingBuffer}).
\end{itemize}

\section{AppState --- корневой контейнер}\label{KT-2.1_Architecture-appstate--ux43aux43eux440ux43dux435ux432ux43eux439-ux43aux43eux43dux442ux435ux439ux43dux435ux440}

AppState --- единый корневой объект, через который проходят все данные между подсистемами.

\begin{longtable}[]{@{}ll@{}}
\toprule\noalign{}
Поле & Тип \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\texttt{platform} & \texttt{PlatformState} \\
\texttt{context} & \texttt{VulkanContextState} \\
\texttt{swapchain} & \texttt{SwapchainState} \\
\texttt{world} & \texttt{WorldState} \\
\texttt{render} & \texttt{RenderState} \\
\texttt{input} & \texttt{InputState} \\
\texttt{interaction} & \texttt{InteractionState} \\
\texttt{simulation} & \texttt{SimulationState} \\
\texttt{ecs} & \texttt{EcsStatePtr} (\texttt{unique\_ptr\textless{}flecs::world\textgreater{}}) \\
\texttt{physics} & \texttt{PhysicsStatePtr} (\texttt{unique\_ptr\textless{}PhysicsWorld\textgreater{}}) \\
\texttt{audio} & \texttt{AudioEnginePtr} (\texttt{unique\_ptr\textless{}AudioEngine\textgreater{}}) \\
\texttt{benchmark} & \texttt{BenchmarkAutomationState} \\
\texttt{lookDevCapture} & \texttt{LookDevCaptureAutomationState} \\
\texttt{frame} & \texttt{FrameState} \\
\end{longtable}

\textbf{Краткое назначение полей:}

\begin{itemize}
\tightlist
\item
  \texttt{platform} --- окно SDL3 и обработчик изменения размера.
\item
  \texttt{context} --- \texttt{VkInstance}, \texttt{Device}, очередь, аллокатор.
\item
  \texttt{swapchain} --- \texttt{VkSwapchainKHR}, представления изображений, extent.
\item
  \texttt{world} --- \texttt{requestedScenePreset}, указатель на \texttt{VoxelWorld}.
\item
  \texttt{render} --- \texttt{sceneResources}, \texttt{frameData}, отладочные оверлеи.
\item
  \texttt{input} --- дельта мыши, снимок действий, replay.
\item
  \texttt{interaction} --- попадание raycast, размещаемый воксель, нормаль и дистанция попадания.
\item
  \texttt{simulation} --- аккумулятор, \texttt{timeScale}, \texttt{frameStep}.
\item
  \texttt{ecs} --- мир flecs (ECS-зеркало).
\item
  \texttt{physics} --- мир Jolt.
\item
  \texttt{audio} --- аудиодвижок с пользовательским удалителем.
\item
  \texttt{benchmark} --- \texttt{frameCount}, средние, минимум/максимум.
\item
  \texttt{lookDevCapture} --- прогрев, список видов для съёмки.
\item
  \texttt{frame} --- профиль CPU, тайминги рендера.
\end{itemize}

\textbf{Связи между ключевыми сущностями:}

\begin{longtable}[]{@{}llll@{}}
\toprule\noalign{}
Связь & Кард. & FK & Назначение \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\texttt{VoxelWorld} → \texttt{VoxelChunk} & 1:N & \texttt{editVersion} & версия мира (инкремент при правке) \\
\texttt{VoxelChunk} ↔ границы & 1:1 & \texttt{min/max} & границы внутри \texttt{VoxelWorld} \\
\texttt{SceneResources} → \texttt{VoxelChunk} & 1:1 & \texttt{chunkDescriptors{[}i{]}} & индекс чанка в \texttt{chunks} \\
\texttt{PhysicsWorld} ↔ \texttt{VoxelWorld} & 1:1 & (read-only) & физ-мир из solid-вокселей \\
\texttt{Renderer} → \texttt{SceneResources} & 1:1 & --- & рендер читает стейтинг \\
\texttt{CharacterVirtual} (Jolt) → \texttt{AppState} & 1:1 & --- & корневой контейнер \\
\end{longtable}

\section{Описание ключевых программных модулей}\label{KT-2.1_Architecture-ux43eux43fux438ux441ux430ux43dux438ux435-ux43aux43bux44eux447ux435ux432ux44bux445-ux43fux440ux43eux433ux440ux430ux43cux43cux43dux44bux445-ux43cux43eux434ux443ux43bux435ux439}

ProjectV декомпозирован на 11 независимых модулей:

\begin{longtable}[]{@{}ll@{}}
\toprule\noalign{}
Модуль & Зона ответственности \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\texttt{Платформа} & Окно (SDL3), обработка событий, \texttt{InputState} \\
\texttt{Воксельный\ мир} & Хранение, редактирование, грязные чанки, raycast \\
\texttt{Физика} & Walk-персонаж (\texttt{CharacterVirtual}), Jolt, синхронизация \\
\texttt{Аудио} & Плейлист, 5-секундное автообновление, горячая замена \\
\texttt{ECS} & Зеркало мира для запросов (MIT/RAII) \\
\texttt{Реестр\ ассетов} & Конвейер glTF / Draco / meshopt, горячая перезагрузка \\
\texttt{Рендерер\ (подготовка\ на\ CPU)} & Мешинг, освещение, данные кадра \\
\texttt{Проекция\ теней} & Тень по сцене, CSM, контактная тень \\
\texttt{Ray\ March} & Параллельный GPU-проход для объёмного рендера \\
\texttt{TAA} & Temporal Anti-Aliasing, исправление гонки дескрипторов \\
\texttt{Vulkan\ (низкий\ уровень)} & Instance, Device, Swapchain, конвейеры, командные буферы \\
\end{longtable}

\textbf{Ключевые файлы и типы по модулям (для быстрой навигации):}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  \texttt{Платформа} --- \texttt{src/platform/PlatformEvents.cpp}, \texttt{InputState.hpp}
\item
  \texttt{Воксельный\ мир} --- \texttt{src/voxel/VoxelWorld.cpp}, \texttt{VoxelWorld.hpp}
\item
  \texttt{Физика} --- \texttt{src/physics/PhysicsWorld.cpp}, \texttt{PhysicsWorld.hpp}
\item
  \texttt{Аудио} --- \texttt{src/audio/AudioEngine.cpp}, \texttt{AudioEngine.hpp}
\item
  \texttt{ECS} --- \texttt{src/ecs/EcsState.*}
\item
  \texttt{Реестр\ ассетов} --- \texttt{src/asset/AssetManifest.cpp}, \texttt{AssetManifest.hpp}
\item
  \texttt{Рендерер\ (подготовка\ на\ CPU)} --- \texttt{src/render/SceneResources.cpp}, \texttt{SceneResources.hpp}
\item
  \texttt{Проекция\ теней} --- \texttt{src/render/ShadowProj.*}
\item
  \texttt{Ray\ March} --- \texttt{src/render/RayMarchPass.*}
\item
  \texttt{TAA} --- \texttt{src/render/Taa.*}
\item
  \texttt{Vulkan\ (низкий\ уровень)} --- \texttt{src/render/vulkan/*}
\end{enumerate}

\section{Описание ключевых алгоритмов}\label{KT-2.1_Architecture-ux43eux43fux438ux441ux430ux43dux438ux435-ux43aux43bux44eux447ux435ux432ux44bux445-ux430ux43bux433ux43eux440ux438ux442ux43cux43eux432}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\item
  \textbf{Игровой цикл} (\texttt{AppMain}). Физика с фиксированным шагом (60 Гц), рендер с переменным шагом, профилирование через Tracy.
\item
  \textbf{Greedy-мешинг} (\texttt{voxel\_mesh.comp}). Объединение соседних solid-вокселей в четырёхугольные грани для снижения перерисовки и indirect-draw.
\item
  \textbf{Полномочия на walk} (\texttt{Физика\ +\ Воксельный\ мир}). \texttt{CharacterVirtual} синхронизирован с \texttt{VoxelWorld.editVersion}; только физика запрашивает столкновения, остальные подсистемы читают \texttt{VoxelWorld} read-only.
\item
  \textbf{Тень по сцене} (\texttt{Проекция\ теней\ +\ voxel\_mesh.comp}). Каскадные карты теней (CSM) рассчитаны по \texttt{sceneChunkVoxelData}, а не по пирамиде видимости --- оптимизация под плотные воксельные сцены.
\item
  \textbf{Клеточный автомат жидкости} (\texttt{voxel\_fluid.comp}). Двусторонний обмен с \texttt{VoxelWorld} для материала \texttt{Fluid=2}: каждый шаг CA читает соседние ячейки, при необходимости обновляет значение.
\end{enumerate}

