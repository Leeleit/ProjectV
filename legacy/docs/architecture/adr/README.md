# ADR Index

Architecture Decision Records from the early legacy phase.

These ADRs are kept for rationale, not as guaranteed current contracts.

| ADR                              | Status                        | Note                                                                                            |
|----------------------------------|-------------------------------|-------------------------------------------------------------------------------------------------|
| `0001-vulkan-renderer.md`        | `historical` / `reference`    | Useful background for the Vulkan-first renderer direction                                       |
| `0002-svo-storage.md`            | `speculative`                 | Describes an R&D storage path, not the current mainline requirement                             |
| `0003-ecs-architecture.md`       | `historical` / `reference`    | Captures early ECS framing and terminology                                                      |
| `0004-build-and-modules-spec.md` | `historical` / `aspirational` | Useful as build/modules intent, but not a statement that the current repo fully uses that model |
