# Keep this directory tracked in git (Doxygen HTML output goes here).
# The HTML is regenerated locally and is intentionally NOT committed
# (per operator decision Q4 in session-2026-06-19T-comment-minimization-r0,
# see agent/status.md §44 + agent/active-sessions.md).
#
# To regenerate:
#
#     doxygen Doxyfile
#
# Then open `docs/api/html/index.html` in a browser.