# <YYYY-MM-DD>-<slug> — <title>

**Status:** _обязательное
поле: `open` / `in-progress` / `concluded-verdict-yes` / `concluded-verdict-no` / `concluded-verdict-mixed` / `parked` /
`abandoned` / `blocked`_
**Date opened:** _YYYY-MM-DD_
**Date closed:** _YYYY-MM-DD или N/A_
**Stage link:** _TODO.md §X.Y / independent_
**Estimated effort:** _XS / S / M / L / XL_
**Author:** _оператор/агент_

---

## 1. Hypothesis

Что я предполагаю. Какое преимущество это даст. Какие альтернативы есть и почему мой подход лучше (или хуже).

_Конкретно:_ «Гипотеза X при условиях Y даст Z (метрика) на сцене W; альтернативы A1/A2 дают меньше из-за причин R1/R2».

---

## 2. Prior art

Web-research. Сначала `web_search` (Exa), затем верификация цитат (открыть первоисточники, проверить год, авторов,
контекст).

Ключевые источники (3–10):

- _ссылка 1 — краткое описание, год, почему важна_
- _ссылка 2 — ..._

Если тема уже частично покрыта в `agent/knowledge.md` или `TODO.md` — cross-ref, не дублировать.

---

## 3. Method

Как я буду проверять гипотезу:

- **Тип эксперимента:** analytical / prototype + benchmark / literature review / mixed.
- **Сцена:** какая именно нагрузка/данные (синтетика / VoxelLab / MeshingStress).
- **Метрики:** что измеряю (mean, p95, FPS, VRAM, и т.п.).
- **Контроль:** baseline (что сравниваю с чем).
- **Протокол:** шаги воспроизведения.

---

## 4. Prototype

Если есть код — где он лежит, как собирается, как запускается, что выводит.

```bash
# команды сборки/запуска
```

Указать, какие части шаблонного harness из `benchmarks/methodology.md` используются.

---

## 5. Results

Цифры, графики, наблюдения, ограничения. Если есть CSV/JSON — приложить.

Таблица (формат свободный):

| Конфигурация | Mean | Median | p95 | p99 | Std | N |
|:-------------|:-----|:-------|:----|:----|:----|:--|
| baseline     |      |        |     |     |     |   |
| hypothesis   |      |        |     |     |     |   |

Наблюдения:

- _что увидели_
- _что НЕ увидели (и почему)_
- _что удивило_

---

## 6. Verdict

`yes` / `no` / `mixed` / `parked` / `abandoned` — одно слово + 2–4 строки обоснования.

---

## 7. Integration recommendation

Что mainline должен сделать (или не делать):

- **Target stage:** _TODO.md §X.Y_
- **Конкретные изменения:** какие файлы/модули/пайплайны затронуты.
- **Подход:** краткое описание реализации.
- **Риски:** что может сломаться, где граница применимости.
- **Критерии приёмки:** как mainline поймёт, что интеграция успешна (>5% прирост по TracyPlot и т.п.).
- **Зависимости:** какие другие стадии/TODO должны быть готовы.
- **Estimated effort:** сколько работы в mainline.

Если вердикт `no` или `mixed` — указать, при каких условиях гипотеза может быть пересмотрена.

---

## 8. Sources

Полный список ссылок (если их больше, чем в §2 — вынести в `sources.md`).

- _ссылка_
- _ссылка_

---

## 9. Mapping to ProjectV hot-path

_Обязательно_, если эксперимент включал измерения:

- Какой участок движка соответствует прототипу.
- Какие допущения/упрощения.
- Что осталось неизмеренным (driver overhead, kernel launch, и т.п.).