#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
cd "${ROOT_DIR}"

echo "=== pre-commit: lint-gate (clang-format + clang-tidy) ==="
exec "${ROOT_DIR}/tools/git/lint-gate.sh" commit
