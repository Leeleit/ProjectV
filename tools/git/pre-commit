#!/bin/bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
cd "${ROOT_DIR}"

echo "=== Local pre-commit gate (linux-clang-debug) ==="
cmake --preset linux-clang-debug
cmake --build --preset linux-clang-debug-build
ctest --preset linux-clang-debug-tests
